{
  "name": "mailgrit-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "MailGrit E2E — Playwright over WebView2 via Chrome DevTools Protocol. Isolated from the Rust build: it does not touch the workspace and does not introduce Node into the build.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "PWDEBUG=1 playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.20.1",
    "typescript": "^7.0.2"
  }
}
